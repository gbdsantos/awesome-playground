import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    flexWrap: "wrap",
    gap: 4,
    paddingBottom: 200,
  },
  ingredientsContent: {
    gap: 12,
    paddingHorizontal: 32,
  }
});
