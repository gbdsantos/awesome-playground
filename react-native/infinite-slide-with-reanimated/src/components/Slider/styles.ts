import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    flexDirection: 'row'
  },
  slide: {
    flexDirection: 'row',
    gap: 42,
    marginRight: 32
  }
});